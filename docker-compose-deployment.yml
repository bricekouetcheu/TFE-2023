version: '3.3'

services:
  nginx:
    depends_on:
      - backend
      - frontend
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./nginx

    ports:
      - "3050:80"

  frontend:
    build:
      context: ./front
    #args:
    #    - REACT_APP_KEY=${REACT_APP_KEY}

    environment:
      - REACT_APP_API_HOST=https://bimcasting.digitalconstruction.cloud/api/
      - REACT_APP_API_KEY=AIzaSyA2qvtdcz2v7p8OhKrMHsds9vJuorlCifY
      - REACT_APP_GOOGLEID=1002668994830-unpc8nig84ourgkekoaa89oph7umtp9q.apps.googleusercontent.com

  backend_concrete:
    build:
      context: ./concretePrediction
    depends_on:
      - db2
      - backend
    healthcheck:
      test: curl --fail http://localhost:8000/health/ || exit 1
      interval: 40s
      timeout: 30s
      retries: 3

  backend_compliance:
    build:
      context: ./service
    depends_on:
      - db2
      - backend
    healthcheck:
      test: curl --fail http://localhost:8000/health/ || exit 1
      interval: 40s
      timeout: 30s
      retries: 3

  backend:
    build:
      context: ./back
    depends_on:
      - db2

    environment:
      - TOKEN_PASS="thisisasecuretoken"
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=TFE_2023
      - DB_HOST=db2
      - DB_PORT=5432
      - FRONTEND_HOST=https://bimcasting.digitalconstruction.cloud/
      - GOOGLE_ID_CLIENT=1002668994830-unpc8nig84ourgkekoaa89oph7umtp9q.apps.googleusercontent.com
      - GOOGLE_CLIENT_SECRET=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

    volumes:
    - IFC:/app/IFC

  db2:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=TFE_2023
    volumes:
      - ./back/databaseFiles/init2.sql:/docker-entrypoint-initdb.d/create_tables.sql
      - db2:/var/lib/postgresql/data

volumes:
  db2:
    driver: local
  IFC:
    driver: local
